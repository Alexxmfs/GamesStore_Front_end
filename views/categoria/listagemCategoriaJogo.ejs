<%- include('../partials/header.ejs')  %>

    <title>LISTAGEM DE CATEGORIAS - LIBRI</title>

</head>

<body>
    
    <div class="container">

        <%- include('../partials/menu.ejs')  %>

        <div class="card mt-2 bg-dark">
            <div class="card-header bg-dark text-light">LISTAGEM DE CATEGORIAS DE JOGOS</div>
        </div>

        <%
            categorias.forEach(categoria=>{
        %>

            <div class="card mt-2 bg-dark">
        
                <div class="card-body bg-dark text-light">
                    <%= categoria.nome_categoria %>
                </div>

                <a href="/formEdicaoCategoriasJogo/<%= categoria.id %>" class="btn btn-success mt-1">EDITAR</a>
                <a href="#" class="btn btn-danger mt-1">EXCLUIR</a>
            </div>
        <% }) %>

    </div>

</body>

<%- include('../partials/footer.ejs')  %>